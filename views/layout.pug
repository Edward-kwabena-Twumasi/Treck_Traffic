block variables
doctype html
html
  head
    meta(charset="utf-8")
    link(rel="shortcut icon", href="/favicon.ico")
    meta(name="viewport", content="width=device-width, initial-scale=1, shrink-to-fit=no")
    meta(name="theme-color", content="#000000")
    title #{title} | Treck  Traffic
    link(rel="stylesheet" href="./styles.css")
  body
    div#root
      block layout-content
    script. 
     console.log("Doing node in script tag") 
     console.log("scrip inside pug");
     
     const arch=document.getElementById("archive") 
     arch.addEventListener('click',()=>{
       alert("Get archives"+document.querySelector("title").innerText)
     },false) 

     const exelDb=document.getElementById("excelDb") 

     excelDb.addEventListener('click',()=>{
      window.loaction.href="/output"

      },false) 
      const jsonDb=document.getElementById("jsonDb") 
      
      jsonDb.addEventListener('click',()=>{
              window.loaction.href="/backup"

      },false)   
            